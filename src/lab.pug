extends includes/base.pug

block append head
  title #{sitename} - Laboratorio
  meta(name="description" content="Creando aplicaciones para traducir numeros y letras a frecuencias, para ver la lógica creativa de este momento")

  - let current = "Lab";

block content
  header.headerContent
    h1.titlePage.
      Gematria App
    p.subTitlePage.
      Para ver la lógica creativa de este momento y las frecuencias de cada cosa.

  include includes/app.pug

  footer.blockquote--pie(style="background-image: url('./img/img3.jpg');")
    
    blockquote
      h3
        | "Sólo cuando bajamos al abismo recuperamos los auténticos tesoros de la vida."
        br
        small &mdash; Joseph Campbell
  
  block append scripts
    //- script(type="module", src="./scripts/app.js")
