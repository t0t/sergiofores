extends includes/baseblog

block append head
  title #{sitename} - Lavadora y procesadora de ideas
  meta(name="description" content="En ocasiones escribo cosas sobre arte, visión holística, crisis creativa, desarrollo de procesos creativos, etc")

  - let current = "Escritos";

block pageheader
  header.headerContent(style="background-image: url('./img/bg8.png');")
    h1.titlePage.
      Artículos
    p.subTitlePage.
      "En ocasiones escribo cosas"

//- block content
  //- ol.postlist.grid
  //-   each article in articles
  //-     li.grid__item.postlist-item
  //-       a.postlist-link(href=`${article.url}`) 
  //-         h3= article.title
  //-         img(src=article.img, alt=article.title)
