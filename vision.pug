extends ./base

block append head
  - var current = "Visión";
  title #{sitename} - #{current}
  meta(name="description" content="Aporto una perspectiva multidimensional, transdisciplinar y holistica de la Creación para encontrarnos a nosotros mismos en el contexto desde el vacío.")

  
block pageheader
  main(title="covertitle")
    h1 !{page_vision.titulo}
  aside(title="coverimg").bgCover(style='background-image: url('+`${baseUrl + page_vision.galeria1[0]}`+');')
  //- img(src=`${baseUrl + page_vision.galeria1[0]}` alt='Cover image')
  header(title="sintesis")
    h2 !{page_vision.subtitulo}

block content
  article.grid-12.grid-md-6
    .slider--container
      .slider1.card
        h3 #{page_vision.headings[4]}
        ol(start="0")
          each frase in page_vision.frases3
            li= `${frase}`

      .slider1.card
        h3 #{page_vision.headings[0]}
        ul
          each frase in page_vision.frases1
            li= `${frase}`

      .slider1.card
        h3 #{page_vision.headings[5]}
        ul 
          each frase in page_vision.frases2
            li= `${frase}`

      .slider1.card
        h3 #{page_vision.headings[3]}
        ul 
          each frase in page_vision.frases4
            li= `${frase}`

      button.prev(onclick='plusDivs(-1, 0)') &#x276E;
      button.next(onclick='plusDivs(1, 0)') &#x276F;

  article.grid-12.grid-md-6
    .slider--container
      each imgUrl in page_vision.galeria2
        .slider2.bgImg(style='background-image: url('+`${baseUrl + imgUrl}`+');')
      button.prev(onclick='plusDivs(-1, 1)') &#x276E;
      button.next(onclick=`plusDivs(1, 1)`) &#x276F;

  article.grid-12.grid-md-6.placeself
    .card
      h3 #{page_vision.headings[2]}
      ul 
        each frase in page_vision.frases5
          li= `${frase}`

  article.grid-12.grid-md-6
    .slider--container
      each imgUrl in page_vision.galeria1
        .slider4.bgImg(style='background-image: url('+`${baseUrl + imgUrl}`+');')
      button.prev(onclick='plusDivs(-1, 3)') &#x276E;
      button.next(onclick='plusDivs(1, 3)') &#x276F;

  article.grid-12.grid-md-6
    .slider--container
      .slider5.card
        h3 #{page_vision.headings[6]}
        ul 
          each frase in page_vision.frases6
            li= `${frase}`
      .slider5.card
        h3 #{page_vision.headings[1]}
        ul 
          each frase in page_vision.frases7
            li= `${frase}`
      .slider5.card
        h3 #{page_vision.headings[7]}
        p NADA
      .slider5.card
        h3 #{page_vision.headings[8]}
        p nada?
      .slider5.card
        h3 #{page_vision.headings[9]}
      button.prev(onclick='plusDivs(-1, 4)') &#x276E;
      button.next(onclick='plusDivs(1, 4)') &#x276F;
  
  article.grid-12.grid-md-6
    .slider--container
      each imgUrl in page_vision.galeria3
        .slider6.bgImg(style='background-image: url('+`${baseUrl + imgUrl}`+');')
      button.prev(onclick='plusDivs(-1, 5)') &#x276E;
      button.next(onclick='plusDivs(1, 5)') &#x276F;

  article.grid-12
    .slider--container.jcc
      .slider3
        iframe(width='560' height='315' src='https://www.youtube-nocookie.com/embed/3RcIpGOs5ZQ?si=so_qptG-vfpNUrAZ' title='YouTube video player' frameborder='0' allow='accelerometer; autoplay; clipboard-write; encrypted-media; gyroscope; picture-in-picture; web-share' allowfullscreen='')
        //- video#video1(poster='./assets/img/bg1.jpg' controls='false' loop='true')
        //-   source(src='./assets/img/introweb.mp4' type='video/mp4')
        //-   | Your browser does not support the video tag. 
      .slider3
        h3.tac Aquí irán llegando más videos...
      button.prev(onclick='plusDivs(-1, 2)') &#x276E;
      button.next(onclick='plusDivs(1, 2)') &#x276F; 

block foot

block append scripts
  script( src="./js/slider.js")